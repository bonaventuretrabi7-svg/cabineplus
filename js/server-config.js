/* ================================================================
   KBINE PLUS | Configuration de l'API serveur (PHP + MySQL)
   ================================================================
   URL de base des scripts PHP déployés sur l'hébergement Hostinger
   existant du site (dossier api/ à la racine, voir api/README.md pour le
   déploiement — aucune ligne de commande nécessaire). Remplace
   js/supabase-config.js (Supabase, abandonné). */
const SERVER_CONFIG = {
  baseUrl: 'https://kbineplus.com/api',
};
